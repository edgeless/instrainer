services:
  web:
    image: node:22-alpine
    working_dir: /app
    ports:
      - "5173:5173"
    volumes:
      - .:/app
    # コンテナ起動時に依存関係をインストールし、Viteの開発サーバーをホストからアクセスできるように立ち上げます
    command: sh -c "npm install && npm run dev -- --host 0.0.0.0"
    environment:
      - NODE_ENV=development
